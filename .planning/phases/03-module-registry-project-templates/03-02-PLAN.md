---
phase: 03-module-registry-project-templates
plan: 02
type: feature
wave: 2
depends_on: [03-01]
requirements: [MOD-01, MOD-02, TPL-01, NFR-04]
files_owned:
  - src/modules/registry.mjs
  - src/modules/schemas.mjs
  - src/modules/loaders.mjs
  - src/modules/module-service.mjs
  - src/repositories/project-repository.mjs
  - src/http/app.mjs
  - test/module-registry.test.mjs
  - test/module-api.test.mjs
autonomous: true
---

# Plan 02: Nine-Module Registry, Read Models, and Draft Configuration API

## Objective

Expose all nine modules through a server allowlist and project/version-safe DTOs, plus transactional draft-only module configuration.

<task type="auto">
  <name>Task 1: Implement schemas, registry, and deterministic loaders</name>
  <files>src/modules/registry.mjs, src/modules/schemas.mjs, src/modules/loaders.mjs, test/module-registry.test.mjs</files>
  <action>Register exactly Overview, Units, Roadmap, Task Network, Gantt, Outcomes, Risks, Metrics, and Materials. Load DTOs from one normalized version graph; compute no invented progress/dates. Validate stable IDs, references, DAG/date rules, schema/view allowlists, and produce a stable envelope containing project/layer/version/template/module metadata. Materials returns the Phase 4 contract state only.</action>
  <verify><automated>node --check src/modules/registry.mjs &amp;&amp; node --check src/modules/schemas.mjs &amp;&amp; node --check src/modules/loaders.mjs &amp;&amp; node --test test/module-registry.test.mjs</automated></verify>
  <done>Nine allowlisted definitions load Xugu and empty standard data; unknown type/view/schema and invalid graph/date/reference inputs fail closed.</done>
</task>

<task type="auto">
  <name>Task 2: Add authorized module repository/service operations</name>
  <files>src/modules/module-service.mjs, src/repositories/project-repository.mjs, test/module-api.test.mjs</files>
  <action>Add sorted manifest/detail reads for published/draft after existing uniform authorization. Add one transactional complete-list draft configuration method validating role, CSRF caller contract, required modules, positions, enable state and view allowlists. Never delete facts and never touch published. Ensure responses omit renderer keys, paths, raw SQL, HTML or code.</action>
  <verify><automated>node --check src/modules/module-service.mjs &amp;&amp; node --check src/repositories/project-repository.mjs &amp;&amp; node --test test/module-api.test.mjs --test-name-pattern="service|repository|draft|isolation"</automated></verify>
  <done>Two projects and all roles remain isolated; draft updates are atomic and published manifests remain byte-equivalent.</done>
</task>

<task type="auto">
  <name>Task 3: Wire module HTTP routes and security matrix</name>
  <files>src/http/app.mjs, test/module-api.test.mjs</files>
  <action>Add GET list/detail endpoints for public/draft modules and PATCH draft module list with existing session/CSRF controls. Missing, archived, unauthorized, disabled and unknown modules use the project-safe 404 contract; invalid body/config uses bounded deterministic 400 responses. Compatibility `/api/public` remains unchanged. Import the isolated populated standard fixture in integration setup and assert its 3-team/non-Xugu task-stage-date data flows through linear roadmap, dependency-list and lane-Gantt DTOs without Xugu values or code changes.</action>
  <verify><automated>node --check src/http/app.mjs &amp;&amp; node --test test/module-api.test.mjs test/auth-project-api.test.mjs test/project-api.test.mjs</automated></verify>
  <done>Module API matrix passes for nine types, populated Xugu and standard projects, public/draft and four roles without cross-project, terminology, date or draft leakage.</done>
</task>
