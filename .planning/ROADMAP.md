# 项目路线图

状态：`accepted`

## Phase 1：项目域与数据底座

状态：`complete` （2026-07-18）

目标：建立可运行的平台骨架、SQLite Schema、项目仓储、版本模型和虚谷项目迁移器。

交付：

- 项目、作战单元、模块、版本、模板和用户基础模型；
- `published / draft / proposal` 三层状态；
- SQLite 初始化、迁移和事务；
- `xugu-agentic-group` 夹具导入与完整性报告；
- 项目级 API 命名空间和旧入口兼容策略；
- 自动化单元与集成测试。

验收：虚谷夹具可进入数据库并按项目 ID 返回 7 个作战单元、29 项任务及完整路线，不修改旧项目。

## Phase 2：平台壳、项目检索与切换

状态：`complete` （2026-07-18）

目标：实现登录后的平台首页和项目级导航。

交付：

- 项目列表、搜索、筛选、最近访问；
- 项目创建、编辑、归档和恢复；
- 项目切换器和项目详情壳；
- 当前虚谷项目通过新 URL 访问；
- 基础角色与会话。

验收：同一用户可在至少两个测试项目之间切换，数据和 URL 不串项目。

## Phase 3：模块注册表与项目模板

状态：`complete` （2026-07-18）

目标：把页面从项目硬编码改造成固定模块 + 数据配置。

交付：

- 九类基础模块 Schema 与渲染器；
- `campaign-map-v1` 和标准项目模板；
- 项目术语、主题、模块启停和排序；
- 路线、甘特、任务网络的通用数据接口；
- 虚谷视觉回归验证。

验收：不用改页面代码即可创建第二个不同名称、不同作战单元的项目并完整展示。

## Phase 4：项目材料与证据层

状态：`complete` （2026-07-18）

目标：将多样文件变成项目隔离、可引用的证据。

交付：

- 项目级上传、预处理和材料台账；
- 页、段、表和图片来源定位；
- 更新模板选择；
- 项目容量、并发、重复和文件门阀；
- 项目内只读问答迁移。

验收：两项目上传同名材料仍保持隔离，问答和证据检索不串库。

## Phase 5：LLM 结构化变更提案

状态：`complete` （2026-07-18）

目标：从材料生成可校验的项目增量，而不是自由文本或页面代码。

交付：

- 会议纪要、计划、进度、指标、成果和新项目六类更新模板；
- `ChangeProposal` Schema；
- 来源、语义类型、置信度和警告；
- 任务依赖、日期、重复、证据和版本冲突校验；
- 生成任务、重试、Token 与成本监控。

验收：参考材料可以生成稳定 JSON；无证据完成状态、循环依赖和跨项目引用全部被拒绝。

## Phase 6：差异审核、发布、权限与运营

状态：`complete` （2026-07-18）

目标：完成低代码更新闭环并达到内部生产试用标准。

交付：

- 按模块差异审核、单项接受/驳回/编辑；
- 草稿预览、事务合并、发布和回滚；
- 完整角色权限与审计；
- 备份恢复、项目导入导出；
- 参考数据集、质量评估和跨项目安全测试；
- 虚谷项目迁移 UAT。

验收：管理员仅通过上传材料和审核提案即可推进虚谷项目路线与甘特，无需修改代码；发布和回滚可审计。

## Phase 7：材料更新可靠性、作战单元生命周期与发布运维

状态：`complete` （2026-07-20）

目标：把“上传材料即可更新项目”的能力从内部试用闭环提升为可发布、可排障、可自检的产品能力。

交付：

- 按更新模板检查材料关键内容覆盖，明确 `ready / warning / blocked` 和关键缺失原因；
- 支持作战单元/团队新增、归档、退出的结构化提案、审核、合并和发布；
- 归档/退出前校验未完成任务、依赖、甘特泳道、战线和证据引用，要求同步关闭、迁移或归档；
- 统一 request/trace ID、脱敏结构化错误事件、堆栈指纹、后台作业关联和管理员诊断包；
- 产品内管理员自检与回归用例中心，覆盖材料更新、关键缺失、作战单元生命周期、权限、发布和故障追踪；
- 浏览器 UAT 覆盖成功更新、缺失材料、单位退出、错误诊断和测试中心。

验收：用户上传符合模板要求的项目材料后，可生成包含任务和作战单元生命周期变化的可审核提案；材料关键内容缺失时产品明确标记并阻止或降级；任一 5xx 或后台失败都能用报障编号拉取脱敏堆栈和关联上下文；管理员可在产品内运行并查看核心回归测试。

## Phase 8：路线图可视化工作台与卡片化审核

状态：`complete` （2026-07-20）

目标：将“看路线、看分支、推进任务、审核材料更新”收敛为同一套可点击、可深链、可审计的视觉工作台；保留 Xugu 作战地图的战略表达，同时补齐时间列活动路线图与阶段卡片管理的可操作性。

交付：

- Roadmap 增加受控视图切换：活动路线图、阶段卡片板、作战单元进度、依赖网络；所有视图从既有版本化项目图读取，不引入项目自定义代码；
- 活动路线图按日期列展示阶段、目标、作战单元分支和任务卡；当前阶段/对象有清晰选中态，路线、单元和任务互相深链；
- 阶段卡片板按任务状态展示可拖拽卡片，卡片含负责人、日期、证据、关键缺失与风险；拖拽只发起结构化变更提案或审核动作，不直接写 `draft/published`；
- 材料生成/审核工作区以“新增、更新、归档、关键缺失”审核卡片呈现 proposal item，保留证据定位、字段差异、编辑后接受、驳回和诊断；
- 多作战单元、多任务和跨阶段依赖在路线、卡片板、单元进度、依赖网络之间同步定位；
- 浏览器 UAT 覆盖视图互跳、就地详情、深链恢复、分支表达、拖拽受控提案、关键缺失和权限/审计边界。

验收：管理员可在一屏内从阶段路线定位作战单元或任务，在卡片板推进一项经授权的提案，在审核卡看到材料证据与缺失字段；任何交互均不把详情藏到页面底部，且未经审核不会改变发布态或草稿。

## 需求覆盖

| 阶段 | 主要需求 |
|---|---|
| Phase 1 | PLAT-03–05、DATA-01–02、NFR-02 |
| Phase 2 | PLAT-01–02、AUTH-01–02 |
| Phase 3 | MOD-01–02、TPL-01–03、NFR-04 |
| Phase 4 | MAT-01–05、CHAT-01–03、NFR-01 |
| Phase 5 | AIU-01–07、NFR-03 |
| Phase 6 | REV-01–05、AUTH-03–04、完整 NFR |
| Phase 7 | AIU-08–09、UNIT-01–03、AUTH-05、NFR-05–07 |
| Phase 8 | VIS-01–05、REV-06 |
