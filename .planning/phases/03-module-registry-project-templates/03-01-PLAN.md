---
phase: 03-module-registry-project-templates
plan: 01
type: feature
wave: 1
depends_on: []
requirements: [TPL-01, TPL-02, TPL-03, NFR-04]
files_owned:
  - src/db/migrations/003_module_registry_templates.sql
  - src/templates/catalog.mjs
  - src/templates/template-validator.mjs
  - src/migration/legacy-project.mjs
  - src/services/project-service.mjs
  - fixtures/projects/standard-project-sample.json
  - scripts/seed-project-fixture.mjs
  - test/template-catalog.test.mjs
  - test/db-foundation.test.mjs
autonomous: true
---

# Plan 01: Versioned Template and Module Persistence Foundation

## Objective

Create one immutable template catalog, migrate stored manifests safely, and make both project templates initialize independent published/draft module graphs without Xugu-specific hardcoding.

<task type="auto">
  <name>Task 1: Add template catalog, manifest validator, and contract tests</name>
  <files>src/templates/catalog.mjs, src/templates/template-validator.mjs, test/template-catalog.test.mjs</files>
  <action>Define immutable `campaign-map-v1@1.0.0` and `standard-project-v1@1.0.0` manifests with theme, terminology, fields, statuses, required modules, nine ordered modules, schema versions, allowlisted view variants, titles, Banner/status copy, and honest empty states. Validate IDs, versions, nine-type uniqueness, normalized positions, required Overview, and known schema/view pairs. Do not expose component paths or executable content.</action>
  <verify><automated>node --check src/templates/catalog.mjs &amp;&amp; node --check src/templates/template-validator.mjs &amp;&amp; node --test test/template-catalog.test.mjs</automated></verify>
  <done>Both manifests validate, unknown/duplicate/mutable/executable-looking configuration is rejected, and standard copy contains no Xugu campaign terms.</done>
</task>

<task type="auto">
  <name>Task 2: Add migration 003 and deterministic backfill tests</name>
  <files>src/db/migrations/003_module_registry_templates.sql, test/db-foundation.test.mjs</files>
  <action>Add version-owned `project_risks` and `project_metrics`, enforce unique module positions, and backfill existing module data JSON with schema/view metadata without altering 001/002 or Xugu facts. Migration must repeat safely, preserve checksums and roll back on invalid state. Keep materials out of persistence for Phase 3.</action>
  <verify><automated>node --test test/db-foundation.test.mjs --test-name-pattern="003|module|template|migration"</automated></verify>
  <done>Fresh and upgraded databases contain the new constraints/entities; repeat is a no-op; Xugu version graphs and 7/29/6/2/4 facts are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Route import and creation through the catalog</name>
  <files>src/migration/legacy-project.mjs, src/services/project-service.mjs, fixtures/projects/standard-project-sample.json, scripts/seed-project-fixture.mjs, test/template-catalog.test.mjs</files>
  <action>Remove duplicated template/module arrays from migration and project service. Resolve catalog manifests, persist exact immutable config, initialize all nine module instances for published and draft with template order/view/schema, and produce template-specific empty snapshots. Add a sanitized standard-project fixture with a different project name, 3 teams, non-Xugu task/stage/workstream counts and dates, plus an explicit isolated-database seed script for API/browser acceptance; production startup must not auto-create it. Preserve legacy export equivalence and project pointer separation.</action>
  <verify><automated>node --check src/migration/legacy-project.mjs &amp;&amp; node --check src/services/project-service.mjs &amp;&amp; node --check scripts/seed-project-fixture.mjs &amp;&amp; node --test test/template-catalog.test.mjs test/project-migration.test.mjs test/auth-project-api.test.mjs</automated></verify>
  <done>Xugu import remains lossless; the populated standard fixture imports with its own counts/dates/terms and nine modules; published/draft configs are distinct version rows.</done>
</task>
