---
phase: 02-platform-shell-project-switching
plan: 01
type: feature
wave: 1
depends_on: []
requirements: [AUTH-01, AUTH-02, NFR-02]
files_owned:
  - src/db/migrations/002_auth_project_access.sql
  - src/security/passwords.mjs
  - src/security/sessions.mjs
  - src/repositories/auth-repository.mjs
  - src/services/auth-service.mjs
  - test/auth-foundation.test.mjs
autonomous: true
---

# Plan 01: Authentication and Session Foundation

## Objective

Add password storage, race-safe bootstrap administration, server-side sessions, CSRF, expiry, and authentication audit primitives.

<task type="auto">
  <name>Task 1: Add authentication persistence and migration tests</name>
  <files>src/db/migrations/002_auth_project_access.sql, test/auth-foundation.test.mjs</files>
  <action>Add nullable login/password fields and platform-admin flag to users; add partial unique login index, sessions, recent_project_access, and append-only audit_events. Preserve migration 001 checksum. Add tests for fresh 001+002, repeat no-op, required constraints/indexes, and rollback.</action>
  <verify><automated>node --test test/auth-foundation.test.mjs --test-name-pattern="migration"</automated></verify>
  <done>Migration tests exist before execution and pass; 001 is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Implement password hashing and race-safe bootstrap with tests</name>
  <files>src/security/passwords.mjs, src/repositories/auth-repository.mjs, src/services/auth-service.mjs, test/auth-foundation.test.mjs</files>
  <action>Implement 12-128 character validation, random salt, scrypt N=2^15/r=8/p=3/64 bytes, timing-safe verification, and dummy unknown-user work. Bootstrap exactly one platform admin inside IMMEDIATE transaction; unique constraints resolve duplicate/concurrent attempts. Add tests for missing/short secret, stored hash shape, verification, repeat no-op, duplicate safety, and no plaintext.</action>
  <verify><automated>node --test test/auth-foundation.test.mjs --test-name-pattern="password|bootstrap"</automated></verify>
  <done>Password/bootstrap cases are implemented and passing without secret-bearing output.</done>
</task>

<task type="auto">
  <name>Task 3: Implement sessions, generic auth, audit, and full foundation tests</name>
  <files>src/security/sessions.mjs, src/repositories/auth-repository.mjs, src/services/auth-service.mjs, test/auth-foundation.test.mjs</files>
  <action>Generate 32-byte tokens, persist only SHA-256, issue HttpOnly SameSite=Strict host cookies with conditional Secure, separate CSRF token, 30-minute idle/8-hour absolute expiry, touch/revoke/logout. Authenticate active users with one generic failure and dummy unknown-user scrypt; rotate session on login and append success/failure/logout audit. Test raw-token absence, cookie, CSRF, expiry, disabled/unknown/wrong login, logout, audit, and deterministic clocks.</action>
  <verify><automated>node --check src/security/passwords.mjs &amp;&amp; node --check src/security/sessions.mjs &amp;&amp; node --check src/repositories/auth-repository.mjs &amp;&amp; node --check src/services/auth-service.mjs &amp;&amp; node --test test/auth-foundation.test.mjs</automated></verify>
  <done>All Plan 01 syntax and tests pass with zero skipped security cases.</done>
</task>

