---
phase: 02-platform-shell-project-switching
plan: 03
type: feature
wave: 3
depends_on: [02-02]
requirements: [PLAT-01, PLAT-02, AUTH-01, AUTH-02, NFR-02]
files_owned:
  - public/index.html
  - public/app.js
  - public/styles.css
  - scripts/verify.mjs
  - test/platform-ui-server.test.mjs
  - package.json
  - README.md
  - docs/ARCHITECTURE.md
  - docs/RESULT.md
  - .planning/DECISIONS.md
  - .planning/PROCESS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/HANDOFF.md
  - .planning/phases/02-platform-shell-project-switching/VERIFICATION.md
  - .planning/evidence/
autonomous: true
---

# Plan 03: Platform UI, Browser Acceptance, and Phase Closure

## Objective

Deliver UI-SPEC, verify safe switching/lifecycle/expiry flows, and close Phase 2 without claiming future modules.

<task type="auto">
  <name>Task 1: Build semantic responsive shell and static contract tests</name>
  <files>public/index.html, public/styles.css, test/platform-ui-server.test.mjs</files>
  <action>Implement login, rail/top bar, project home, project shell, dialogs, status regions, disabled future navigation, exact copy, tokens, 60/30/10 color, spacing/type rules, focus/reduced motion, and 1440/1024/390 breakpoints with no remote/inline executable assets. Add tests for landmarks/copy, local assets, CSP/no-store/MIME, direct /login,/projects,/projects/:id routes, and no project data before login.</action>
  <verify><automated>node --test test/platform-ui-server.test.mjs --test-name-pattern="HTML|CSS|security headers|direct route"</automated></verify>
  <done>Static tests are created with implementation and pass for semantic/security/responsive contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement client routing, switching, lifecycle, expiry safety, and tests</name>
  <files>public/app.js, test/platform-ui-server.test.mjs</files>
  <action>Keep session/CSRF in memory; route login/projects/detail; mirror search/status/sort URL; render exact schemas with textContent; recent top-4; active switcher; null progress copy; no viewer draft. Add admin lifecycle dialogs/CSRF. On 401 clear memory, preserve one safe intended path, replace to login with expiry message, one-time replace back after login. Logout clears state/replace; pageshow revalidates so Back reveals no cached facts. Add contract tests for safe rendering, URL/data switch, lifecycle, expiry intent, logout cache, no innerHTML data sink/Web Storage credential.</action>
  <verify><automated>node --check public/app.js &amp;&amp; node --test test/platform-ui-server.test.mjs</automated></verify>
  <done>Client/static suite passes; switching synchronizes facts+URL; expiry/logout Back contracts are asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Unified verification, three-viewport browser acceptance, and project closure</name>
  <files>scripts/verify.mjs, package.json, README.md, docs/ARCHITECTURE.md, docs/RESULT.md, .planning/DECISIONS.md, .planning/PROCESS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/HANDOFF.md, .planning/phases/02-platform-shell-project-switching/VERIFICATION.md, .planning/evidence/</files>
  <action>Extend verify to all Phase 1/2 tests, migrations, Xugu equivalence, auth/API/static smoke, sensitive tracking, and reference read-only checks. Run all 12 UI-SPEC checks at 1440x900, 1024x768, 390x844 including keyboard, applicable 200% zoom, create/switch/archive/restore/viewer denial/session expiry/logout Back; save QA only under evidence. Bump version and document implemented results, security limits, and Phase 3 next step.</action>
  <verify><automated>npm run verify</automated></verify>
  <done>Automated and browser matrices pass; Phase 2 VERIFICATION maps requirements; result/state/roadmap/handoff agree; worktree is clean after atomic commits.</done>
</task>

