# Plan 06-01：审核与版本数据底座

1. Migration 006 adds normalized review, merge and publication event entities with composite project constraints.
2. Add version graph clone/read/apply primitives that preserve every normalized row and validate before pointer changes.
3. Add deterministic tests for migrations, clone fidelity and transaction rollback.
