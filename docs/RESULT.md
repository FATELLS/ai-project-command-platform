# 当前结果

> 本文件只描述已经完成并通过验证的结果。规划目标见 `.planning/`。

## 已完成

- 建立独立项目、需求、架构、路线图、AI 契约和只读迁移基线。
- 建立 Node.js 24.15+ 与内置 SQLite 数据底座，包含迁移校验和、WAL、外键、索引和事务。
- 项目、模板、用户、成员、版本、模块、作战单元、路线、闭环、任务、依赖、战线和提案元数据使用独立实体。
- `published` 与 `draft` 使用独立版本图；项目指针受归属和层级约束。
- `xugu-agentic-group` 脱敏 v4.2 已迁移，保留 7 个作战单元、29 项任务、6 个节点、2 个闭环和 4 条公司战线，导出与夹具语义等价。
- 密码使用带盐 scrypt；会话 token 只以 SHA-256 摘要保存，浏览器使用 HttpOnly、SameSite=Strict Cookie。
- 会话有 4 小时空闲与 8 小时绝对过期，写操作使用内存 CSRF token，登录有统一错误与 IP 窗口限流。
- 建立平台管理员、项目管理员、项目编辑者、查看者角色；项目列表和详情按 `projectId` 与成员权限隔离。
- 建立授权项目搜索、状态筛选、排序、最近访问前四、项目切换和只读发布态概览。
- 平台管理员可事务性创建、编辑、归档和恢复项目；每次生命周期变化写入追加式审计事件。
- 创建项目生成相互独立的空发布版/草稿版，并授予创建者项目管理员角色；不写虚构完成率。
- 提供本地语义 HTML/CSS/JavaScript 平台 UI，含登录、项目首页、项目壳和可访问管理对话框；桌面端已按 Xugu 稳定界面修正为 76px 白色顶部导航、暖色指挥背景、左右双栏 Hero 和横向章节卡片，无深色 SaaS 侧栏。
- 项目详情的顶部标题、Banner、当前状态提示、事实标签、模块标签和新建摘要已由项目模板/术语配置驱动；`xugu-agentic-group` 作为首个 Xugu Agentic Group Schedule 作战项目保留作战语言，标准项目使用团队、任务、里程碑和交付物语言。
- 提供不可变、版本化的 `campaign-map-v1@1.0.0` 与 `standard-project-v1@1.0.0` 目录；模板定义受控主题、术语、字段、状态、必需模块、顺序、Schema 和默认视图。
- 服务端注册 Overview、Units、Roadmap、Task Network、Gantt、Outcomes、Risks、Metrics、Materials 九种模块；未知类型、Schema、视图、关系和可执行资产失败关闭。
- 浏览器通过仓库内固定 DOM/SVG renderer map 展示九类模块；项目配置不能提供 HTML、CSS、JavaScript、SVG 或组件路径。
- 模块配置采用完整列表事务，只写 `draft`；Overview 固定启用，保存草稿不改变 `published` 导航或删除事实，查看者无配置入口。
- Xugu Roadmap/Task Network/Gantt/Outcomes 保留 6 阶段、2 闭环、4 战线、29 任务和 28 有日期/1 待排期语义；标准夹具独立显示 3 团队、7 任务、4 里程碑、2 工作流及线性/依赖列表/泳道变体。
- Xugu 战役路线图使用固定可配置 renderer 呈现曲线路线；主图只显示战役节点，并在当前节点上直接显示可点击的作战单元分支胶囊和任务数。节点可点击并深链到 `?stage=...`，分支胶囊可深链到 `?stage=...&unit=...` 并将对应分支详情高亮置顶；下方节点详情显示说明、预期产出、本地预览资源，以及按作战单元和任务日期推断出的分支任务；分支任务会在原位展开详情并深链到 `?stage=...&task=...`。
- 作战单元模块点击“查看作战单元详情”后在当前卡片内展开该单元路线、战役节点和支线任务；点击单元支线任务同样在原位展示任务详情，不再把结果隐藏到页面底部。
- Materials 模块已成为固定项目材料工作区，提供服务器权威配额、流式文件上传、人工材料、持久台账、状态筛选、处理重试、更新意图和独立问答授权。
- 摄入层校验文件大小、魔数/MIME、压缩路径与展开上限、容量、频率和并发；可选扫描器配置为必需时不可用即失败关闭，失败流不会留下孤儿对象。
- 证据处理支持人工文本、TXT、JSON、CSV、DOCX、PPTX、XLSX，以及受限 PDF/图片子进程适配器；证据保留段、页、幻灯片、单元格、边界框或 OCR 定位和稳定摘要。
- 项目问答使用同一 SQLite 事务域内的项目/授权/当前代际过滤与 FTS5 检索；provider 默认禁用，证据视为不可信文本，输出只接受 allowlist 引用，无证据或 provider 不可用时诚实拒绝。
- Chat 与未来 Generation 使用独立持久配额和全局有界 provider 并发；Phase 4 问答只读，不写 proposal、draft 或 published。
- 六类更新模板以不可变 `id@version` 目录约束会议纪要、项目计划、进度汇报、指标数据、成果归档和新项目材料可生成的模块、操作、字段与高影响证据要求。
- 生成任务锁定当前项目发布版本、同模板材料、当前提取代际、证据 ID/哈希和 Schema；支持幂等创建、失败重试 lineage、stale 基准、attempt、Token 和版本化成本记录。
- generation 使用独立服务端 provider 配置、4 次/分钟与 100 次/日预算，并与 chat 分账；无配置时材料和既有提案仍可浏览，提交明确禁用。
- `change-proposal-v1@1.0.0` 只接受有界结构化增量；服务端重新校验 envelope、项目/证据归属、模板字段、目标、语义、日期、依赖 DAG、重复与版本冲突，非法输出最多修复一次后失败关闭。
- 仅完整验证通过的提案、变更项和证据关系在一个事务中保存；所有生成成功、失败和重试路径均无权修改 `draft`、`published` 或项目版本指针。
- 固定 Materials renderer 提供“AI 生成项目节点预览”工作区、生成任务详情、业务变化卡片、验证结果、字段差异、置信度/警告与精确证据回跳；`ChangeProposal` 只保留为后端领域术语。
- 审核决定与提案项分离保存；管理员可逐项接受、驳回、字段绑定编辑后接受或整模块接受，编辑值会重新执行 Schema、证据、日期、任务归属和依赖校验。
- 接受项使用 copy-on-write 新草稿版本事务合并；版本复制、字段应用、关系约束、完整图校验、校验和和草稿指针切换同成同败，AI 无合并权限。
- 发布中心确定性比较当前 `published` 与 `draft`，显示按模块差异、结构检查、未决审核和直达发布禁令；人工确认后创建新发布版本及独立草稿基线。
- 回滚只接受当前发布事件的直接前驱，回滚后建立新草稿基线；审核、合并、发布、回滚、用户和成员动作进入追加式审计。
- 平台管理员可通过项目隔离 API 创建/停用用户并管理成员角色；项目管理员可管理本项目编辑者/查看者，最后一个平台/项目管理员受保护。
- 提供一致 SQLite `VACUUM INTO` 备份、完整性/外键/迁移校验和离线恢复前自动备份；项目导入支持显式模板且导出不包含密钥或运行材料。
- 材料关键内容覆盖按六类更新模板给出 `ready / warning / blocked` 诊断；材料台账、材料详情、生成准入和生成上下文都会显示或保存关键缺失、建议补充项与诊断建议。
- 生成任务在锁定材料时保存 readiness 快照；关键内容缺失会在调用 provider 前以 `MATERIAL_READINESS_BLOCKED` 阻止生成，避免用缺字段材料更新项目。
- 作战单元支持 `active`、`archived`、`exited` 生命周期；新增、归档和退出只能通过带证据的结构化提案进入审核，删除作战单元被拒绝。
- 归档或退出作战单元必须提供生效日期、原因和证据；若仍有未关闭任务，必须在同一提案中关闭、完成或归档相关任务，否则合并前校验失败。
- 所有 HTTP 响应带 `x-request-id`；未知 5xx 会写入脱敏错误事件，保存 request/trace、路由、用户、项目、状态、错误指纹、堆栈和关联上下文。
- 平台管理员和授权项目管理员可在 Materials 工作区“运维自检”中运行核心产品测试，查看测试运行、最近错误和可复制诊断包；查看者无法访问。
- Phase 6 参考集覆盖 10 个 grounding、隔离、版本、图、日期、重复、审核、事务和发布场景；非法任务归属在审核编辑保存时失败关闭。
- 直接刷新项目路由、会话过期、退出后 Back、未知/越权统一 404 已通过真实浏览器验证。
- 统一验证通过 157 项自动化测试、35 项 Playwright 全自动浏览器 E2E，以及 Phase 3–6 共 63 个浏览器阻断用例；19 张证据均通过 SHA-256 与尺寸机检。
- Phase 7 本机浏览器抽查确认虚谷项目 Materials 模块、运维自检入口和 `5/5` 核心自检运行可用。
- 验证确认参考 Xugu 应用的 HEAD、Git 状态和脱敏种子哈希未变。

- Roadmap 模块以卡片式"项目路线图"为主视图，并提供作战单元进度辅助投影。两个入口共享同一版本化项目图投影（`loadRoadmap` 输出 units/tasks/edges），切换不改变 published/draft/proposal 边界。
- 项目路线图（内部兼容值 `swimlane`）已按用户确认的 A 方案重构为“主任务时间线 + Trello 式副任务卡片”：未选主任务时隐藏全部副任务；点击主任务后在原位展开说明/预期产出，并只显示相关作战单元；副任务固定宽度锚定在主任务阶段列，不按工期拉伸；点击副任务在原位置展开详情，再次点击收起。作战单元采用稳定受控差异色，状态同时使用文字/视觉提示；`stage/unit/task/anchor` 深链、生命周期模板术语、同单元拆解链和多源收口继续生效。该视图纯投影渲染，不改隔离与数据边界。
- 卡片泳道折叠态已收敛为标题优先：未展开主任务和副任务均只显示单行标题，日期、状态、负责人、任务数、说明和产出进入展开态；选中主任务单独增高，不再把相邻卡片等高撑大。
- Phase 9 切片一为泳道补同单元 `parentId` 真实拆解链：带父任务的子任务条标记 `⇢` 拆解指示，选中任务时高亮整条拆解链（父链+子链）、非链任务淡化；parentId 与 dependsOn 在 validator 合并去重，不得重复。种子补 4 条同单元拆解链（研发/技术/财务），迁移来源基线同步更新。
- Phase 9 切片二（LIF-01）让泳道生命周期三带术语随项目模板配置：虚谷（campaign）保持「事前/事中/事后」作战语言，标准项目（standard）用「规划/执行/交付」通用项目管理语言；项目层 terminology 可覆盖。渲染器从 presentation 读，删除硬编码常量。
- Phase 9 切片三（LIF-04）让收口锚点支持多源合并：锚点位置取所有 between 阶段的中点均值（原只取前两个），一个收口可跨多个阶段；种子补一条 3 元素 between 收口验证多对多收口。
- Phase 9 切片四（LIF-05）让生命周期范式分形下沉到作战单元：副泳道行头显示每个单元自己的事前/事中/事后带标记，由该单元任务的状态/progress/排期派生（零数据扩展），视觉表明同一范式从项目级下沉到单元级。
- 阶段卡片板任务卡可拖拽（仅编辑者及以上），拖拽只创建 `interaction` 模板 ChangeProposal，绝不直写 draft/published；高影响字段（state/owner/日期）必须引用本项目已就绪材料证据，经审核与 copy-on-write 草稿合并后才生效。
- 新增交互提案服务路径 `createInteractionProposal`：锁定当前 published、复用 validator、保存为 pending，不调用 LLM、不创建 generation job；缺 CSRF 返回 403，viewer 返回统一 404，伪造/跨项目证据返回 EVIDENCE_NOT_ALLOWED。
- 审核工作区把每项 proposal item 呈现为审核卡片（字段差异、证据定位、置信度、警告、接受/驳回/编辑后接受），整模块接受与事务合并沿用 Phase 6 边界。
- 路线节点、分支、作战单元、任务和审核卡片均有可见可点击性、键盘访问、选中态、就地详情和可复制深链（`?view=`/`?stage=`/`?unit=`/`?task=` 正交）。
- 用户反馈：会话空闲超时由 30 分钟调至 4 小时（绝对超时仍为 8 小时硬上限），避免短时离开即过期。
- 卡片泳道即「项目路线图」；独立“活动路线图”曲线已退役，旧 `?view=timeline` 回落到项目路线图。独立「排期甘特」工作区继续承担精确工期表达。
- 项目一级导航已从九个技术模块收敛为六个用户工作区：作战/项目总览、项目路线图、作战单元/团队、排期甘特、项目健康和项目资料。任务依赖并入路线图辅助视图；风险/指标进入项目健康二级导航；战果与材料台账进入项目资料二级导航，AI 节点预览由独立项目更新流程承接。底层九类固定模块、API、权限、配置与深链保持兼容。
- 卡片泳道所在的 16 条路线图 Playwright 用例已通过，覆盖默认隐藏、主任务展开/收起、固定卡片宽度、单元差异色、子任务原位展开、深链恢复、模板术语和多源收口。
- 建立非标准项目材料 UI Benchmark：通过产品页面上传周会 DOCX、跨单元 XLSX 台账、群聊 TXT、邮件转发链 TXT 和供应商 JSON，分别形成 50、113、14、33、25 个可定位证据块；不支持 EML 与重复 DOCX 均在 UI 明确拦截，台账最终保持 5 条。
- 正式服务入口现在随 Web 进程启动串行材料处理 worker，并在启动时恢复遗留任务、异常退避和退出时优雅停止；无需额外后台命令即可完成 UI 上传后的异步预处理。
- XLSX 提取兼容带 namespace 前缀的 Office XML，证据详情显示精确工作表/单元格；MD、CSV、JSON、YAML 的 UI 声明与上传门阀已统一，重复/容量/并发等门阀错误均有中文提示。
- 生成面板现显示材料 readiness；provider 禁用时诚实阻止生成，证据超过上限时由服务端安全截断而不被前端误禁用。
- BM-08 已用真实 GLM-5.2 从 UI 跑通“项目计划材料→带证据结构化提案→逐项人工审核→事务合并→人工发布→路线图验收”：一次生成 3 个 roadmap create 项、4317 Token，发布版本从 `v4.2` 升为 `v4.3`。LLM 没有审核、合并或发布权限。
- 路线图末端主任务展开态已修正横向越界：画布宽度完整计入列间距与 padding，深链恢复或点击末端节点时会自动滚入局部安全区，右侧后续节点保持完整可见。
- 主任务和副任务详情采用覆盖相邻列的宽卡展开：保持时间线与折叠卡列宽不变，中间卡居中覆盖、首尾卡向内覆盖，详情更宽且显著减少纵向高度。
- 打开主任务后，路线画布以低强度深蓝灰遮罩降亮非当前区域；主任务使用更明显的半透明玻璃背景，所属副任务收纳在同宽 420px 面板内按三列排列。作战单元收成每组顶部小色标，主卡与子任务区用平滑双坡线连接，二级详情不越过面板边界。
- `0.8.0` 新增 Windows x64 ZIP、Linux x64 tar.gz 与 RHEL 系 x64 RPM 分发；安装包自带 Node.js，RPM 自动注册 systemd 并启动。Release 只复制运行白名单，不包含项目数据库、上传材料、真实密钥、夹具、规划和测试内容。
- 私有 GitHub 仓库与 `v0.8.0` Release 已完成发布；Windows/Linux 原生 x64 启动冒烟、RPM 构建和 SHA-256 产物上传全部成功。
- Phase 10 用户体验简化已落地：材料工作区消除抬头重复（移除 localTabs，sectionNav 统一覆盖全部子视图）；战情问答从材料工作区标签移出为项目级右侧浮动按钮（所有项目页面可用）；材料表格和详情页彻底移除问答授权/生成授权列、按钮和元数据行，上传处理、人工录入和模板选择均在内部自动开启生成资格；项目创建提供三种方式（对话式 AI 引导、上传材料自动提取、手动表单），新增 `/api/projects/from-material` 和 `/api/projects/suggest` 端点。
- 2026-07-29 更新后验证修复已落地：`outcomes` 已恢复为九类固定模块之一并接入模板、服务端 loader、前端 renderer 和“项目资料”导航；登录 IP 窗口限流恢复；PDF/图片材料在无 vision provider 时仍可走受限 `pdftotext`/`tesseract` fallback；迁移 008–010 已纳入备份与统一验证基线。
- Phase 11 第一实施切片已落地：项目入口改为紧凑操作工作台；项目内固定为六个一级工作区和单一标题层级；总览、材料和审核页把当前业务对象与主操作移到首屏；查看者界面隐藏技术 ID 与受限入口；三种创建方式统一经过项目骨架确认；材料状态改为用户可行动语言；审核先展示业务变化、影响与证据并折叠技术细节；设置新增渐进披露和不回显密钥的连接测试；移动端不再隐藏路线图核心内容。
- Phase 11 视觉基础已收敛：缩小装饰性 Hero、阴影与圆角，提升正文、导航和控件尺寸，统一 44px 触摸目标、业务详情折叠和审核粘性操作区；1280×720 首屏、1440×900 桌面和 390×844 手机自动化检查通过。
- Phase 11 后已从隔离产品 UI 重跑多文档到路线图闭环：DOCX、XLSX、两种 TXT、JSON 完成摄入，EML 与重复材料正确拦截；项目计划材料真实生成 3 个路线节点，经逐项审核、草稿合并和人工发布后出现在路线图。复跑同时修复同一上传面板无法连续上传，以及 proposal `date` 发布后显示“时间待确认”的回归。
- 源码运行新增受管理后台生命周期：`start:background / status / stop / restart`；停止以 SIGTERM 依次关闭 HTTP、材料 worker 和平台数据库连接，不会停止虚谷数据库或 Docker 容器。总览主动作已从“上传项目材料”调整为“项目更新”。
- 项目创建与材料输入已共享六类可下载 Markdown 模板：会议纪要、项目计划、进展汇报、指标数据、成果归档和项目创建材料。模板入口覆盖创建、总览、材料台账、上传/人工录入、生成和材料详情，下载不会触发项目 mutation。
- 创建材料 API 现与更新材料入口对空文件、扩展名/MIME 和内容签名执行对称门阀；文本材料缺少项目名称/Markdown 标题或项目目标时不进入骨架确认。合法 Markdown 标题会完整提取，不再退化为单字符。
- 独立异常材料 UI 套件覆盖空文件、伪装 PDF、未知格式、重复材料、缺少创建要素和阶段用途不匹配；失败不增加项目、材料或生成任务，也不改变 published。
- “AI 生成项目节点预览”已成为独立项目更新流程；项目资料只保留战果档案和长期材料台账。通用 `/updates` 固定从本次材料开始，依次进入处理与生成、绑定具体 proposal 的模拟路线图、逐项审核和人工发布；历史待处理更新仅作为次级继续入口。
- 模拟路线图只显示一张当前发布图叠加本次变化后的路线图，AI 新增、修改和当前节点分色，日期不足的本次 AI 卡片进入同一 renderer 的待排期区；驳回会移出投影，编辑后接受按修改值重算。
- 项目更新路线图直接复用正式 `renderRoadmapSwimlane`，通过 preview mode 限制编辑范围；生成统计、批量生成、提案列表、生成任务列表、模板控制台以及旧专用预览 DOM/CSS 均已移除。
- 节点路线图预览不创建版本、不移动项目指针，也不写入 draft/published；审核、事务合并和人工发布仍是独立步骤。
- 路线图阶段和任务卡片均提供共享字段编辑器，覆盖标题、描述、日期、状态、负责人、进度、健康度、依赖、交付物、风险、验收标准等属性；数组属性支持逐项增删改。
- 正式路线图编辑与删除只创建受控 `interaction` ChangeProposal，关键字段和删除要求项目材料证据，不直接写入 draft/published；派生阶段会定位并编辑真实来源任务。
- AI 节点预览只允许编辑本次 proposal 生成或修改的卡片；既有发布节点使用降亮样式和明确只读标签。删除本次预览卡片等价于驳回该候选变化，不会删除正式路线节点。
- 两处删除均要求输入精确短语“确认删除”后才启用操作。
- 顶栏工具、模块滚动、路线图图例/状态、项目创建方式和卡片编辑已统一为共享线性 SVG 图标；路线图编辑使用卡片内侧铅笔按钮，标题与状态不会被工具位遮挡，纯图标具备可访问名称与 tooltip。
- 全量验证通过：182 项后台/静态测试 + 49 项主流程 Playwright E2E + 9 项隔离异常材料与节点预览 Playwright E2E 全绿；Phase 3–6 浏览器证据、Xugu 等价和参考来源只读校验通过。

## 尚未实现

- 独立的完整用户目录管理页面（首版已有安全 API 和发布中心成员只读表）；
- Phase 11 完整注意力队列、查看者服务端提案读取收紧、团队/阶段/任务级项目骨架、单动作受控发布编排，以及全角色/模板键盘与视觉矩阵；
- PostgreSQL、多服务器部署、实时多人协同和任意自定义代码模块。

## 当前版本

- 平台版本：`0.8.0`
- 已完成阶段：Phase 1–10 + Phase 11 第一实施切片 + 全自动浏览器 E2E 层（Playwright）
- 当前 Git 远程：`origin` 指向 `FATELLS/ai-project-command-platform`
