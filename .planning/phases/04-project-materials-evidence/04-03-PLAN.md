---
phase: 04-project-materials-evidence
plan: 03
type: feature
wave: 3
depends_on: [04-02]
requirements: [CHAT-01, CHAT-02, CHAT-03, AUTH-04, NFR-03]
files_owned:
  - src/ai
  - src/services/chat-service.mjs
  - test/chat-retrieval.test.mjs
  - test/chat-provider.test.mjs
  - test/ai-quota.test.mjs
autonomous: true
---

# Plan 03: Deterministic Cited Read-only Q&A

## Objective

Implement `rag-v1` as a no-tools, project-scoped service whose answers can only cite retrieved authorized evidence and cannot affect project state.

<task type="auto">
  <name>Task 1: Add retrieval, prompt and answer validation</name>
  <files>src/ai/retriever.mjs, src/ai/prompt-builder.mjs, src/ai/answer-validator.mjs, test/chat-retrieval.test.mjs</files>
  <action>Build safe FTS MATCH terms, stable top-8 ranking, published facts context, strict untrusted-evidence boundaries and schema/citation allowlist validation. Zero/weak evidence returns deterministic insufficiency without provider call; material prompt injection cannot change project, permissions or structure.</action>
  <verify><automated>node --test test/chat-retrieval.test.mjs</automated></verify>
  <done>Reference recall/citation/refusal/isolation cases meet EVAL.md and unknown citations fail closed.</done>
</task>

<task type="auto">
  <name>Task 2: Add provider adapters, quotas and chat orchestration</name>
  <files>src/ai/providers, src/ai/provider-factory.mjs, src/ai/quota.mjs, src/services/chat-service.mjs, test/chat-provider.test.mjs, test/ai-quota.test.mjs</files>
  <action>Implement disabled and test-injected fake providers plus a server-only OpenAI-compatible fetch adapter with HTTPS hostname allowlist, abort deadline, bounded retry/response/output, no tools and redacted errors. Atomically reserve separate chat quotas and global semaphore before calls; release/record all exits. Do not expose fake through production env.</action>
  <verify><automated>node --test test/chat-provider.test.mjs test/ai-quota.test.mjs</automated></verify>
  <done>No-key startup works; timeout/429/5xx/invalid/tool-call cases are stable; chat/generation accounting remains separate across restart.</done>
</task>
