---
phase: 05-structured-change-proposals
plan: 02
type: feature
wave: 2
depends_on: [05-01]
requirements: [AIU-01, AIU-02, AIU-03, AIU-04, AIU-05, AIU-06, AIU-07, AUTH-03, AUTH-04, NFR-03]
files_owned:
  - src/proposals/context-builder.mjs
  - src/proposals/prompt-builder.mjs
  - src/proposals/validator.mjs
  - src/proposals/generation-service.mjs
  - src/ai/provider-factory.mjs
  - src/ai/providers/openai-compatible-provider.mjs
  - test/proposal-validator.test.mjs
  - test/generation-service.test.mjs
  - test/generation-provider.test.mjs
autonomous: true
---

# Plan 02: Deterministic Validation and Generation Jobs

## Objective

Turn selected ready materials into a locked, bounded provider context and persist only fully validated delta proposals while every failure remains recoverable and read-only.

<task type="auto">
  <name>Task 1: Build server-owned context and adversarial validator</name>
  <files>src/proposals/context-builder.mjs, src/proposals/prompt-builder.mjs, src/proposals/validator.mjs, test/proposal-validator.test.mjs</files>
  <action>Resolve route project, current published graph, one update template, selected ready/current-generation materials and bounded evidence allowlist. Validate exact project/base/template/material equality, evidence, semantic/high-impact rules, module fields/targets, ISO dates, resulting task DAG/same-unit links, duplicate normalized names/IDs and conflicting operations. Append deterministic warnings without silently rewriting output.</action>
  <verify><automated>node --test test/proposal-validator.test.mjs</automated></verify>
  <done>All valid six-template fixtures pass; cross-project, stale, no-evidence fact, cyclic, bad-date, duplicate/collision and prompt-injection fixtures fail with stable codes.</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrate leases, strict provider output, repair and usage</name>
  <files>src/proposals/generation-service.mjs, src/ai/provider-factory.mjs, src/ai/providers/openai-compatible-provider.mjs, test/generation-service.test.mjs, test/generation-provider.test.mjs</files>
  <action>Create/reserve generation jobs atomically, claim expiring leases, build context, call a generation-specific no-tools provider, optionally repair invalid structure once, record every attempt/token/latency/pricing state and persist proposal/items/evidence in one transaction. Retrying a terminal success is idempotent; stale base never rebases. Disabled/provider/abort/invalid exits create durable safe state and never update draft/published.</action>
  <verify><automated>node --test test/generation-service.test.mjs test/generation-provider.test.mjs test/ai-quota.test.mjs</automated></verify>
  <done>No-key and adversarial provider matrix passes; chat/generation accounting stays separate; proposal persistence is atomic and version pointers/hashes are unchanged.</done>
</task>
