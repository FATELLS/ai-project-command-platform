AI Project Command Platform · Windows x64

1. 解压整个 ZIP，不要只复制 Start.ps1。
2. 右键 Start.ps1，选择“使用 PowerShell 运行”。
3. 浏览器会打开 http://127.0.0.1:4173 。
4. 首次管理员凭据会显示在窗口中，并写入 first-run-credentials.txt。
5. 使用 Stop.ps1 停止服务。

程序不会附带任何项目数据库、上传材料或 API Key。运行数据保存在解压目录的 data 文件夹中。
