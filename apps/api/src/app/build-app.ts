/**
 * Fastify App Builder (skeleton)
 *
 * G05 will implement the real Fastify assembly.
 */

export interface AppOptions {
  /** TODO(G05): logger, database connection, etc. */
}

export async function buildApp(_options?: AppOptions): Promise<unknown> {
  // TODO(G05): create Fastify instance, register plugins, mount routes
  return null;
}
