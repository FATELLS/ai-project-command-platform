---
phase: 03-module-registry-project-templates
plan: 03
type: feature
wave: 3
depends_on: [03-02]
requirements: [MOD-01, MOD-02, TPL-02, TPL-03]
files_owned:
  - public/app.js
  - public/styles.css
  - public/modules/registry.js
  - public/modules/shared.js
  - public/modules/renderers.js
  - test/module-ui-server.test.mjs
  - test/platform-ui-server.test.mjs
autonomous: true
---

# Plan 03: Fixed Module Renderers and Xugu-Aligned Module UI

## Objective

Turn the Phase 2 shell into data-driven module routes using only repository-local safe DOM/SVG renderers for both templates.

<task type="auto">
  <name>Task 1: Add client registry, safe shared states, and route loading</name>
  <files>public/modules/registry.js, public/modules/shared.js, public/app.js, test/module-ui-server.test.mjs</files>
  <action>Add a fixed nine-type renderer map, module routes, manifest-driven horizontal navigation, loading/empty/error/stale states, version/project envelope checks, direct refresh and Back/Forward. All project strings use DOM text APIs; unknown schemas/views fail closed. Disabled modules are omitted rather than shown as future placeholders.</action>
  <verify><automated>node --check public/modules/registry.js &amp;&amp; node --check public/modules/shared.js &amp;&amp; node --check public/app.js &amp;&amp; node --test test/module-ui-server.test.mjs --test-name-pattern="registry|route|state|safe"</automated></verify>
  <done>Every enabled module has a canonical route and honest state; no project HTML/SVG/URL can choose or execute code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement nine fixed renderers and visualization variants</name>
  <files>public/modules/renderers.js, public/styles.css, test/module-ui-server.test.mjs</files>
  <action>Implement Overview, Units, Roadmap, Task Network, Gantt, Outcomes, Risks, Metrics and Materials. Recreate Xugu campaign route/branch/merge/Gantt/outcome semantics with data-derived SVG/DOM geometry and textual alternatives; implement standard linear/dependency-list/lane/deliverable variants. Handle arbitrary counts/dates, unscheduled tasks, null metrics/progress and exact empty copy.</action>
  <verify><automated>node --check public/modules/renderers.js &amp;&amp; node --test test/module-ui-server.test.mjs --test-name-pattern="overview|units|roadmap|network|gantt|outcomes|risks|metrics|materials"</automated></verify>
  <done>Both populated template fixtures render nine modules from the same fixed code; Xugu facts and the standard fixture's 3-team/arbitrary task-stage-date data are driven only by DTOs with accessible alternatives.</done>
</task>

<task type="auto">
  <name>Task 3: Add draft configuration sheet and responsive/accessibility contract</name>
  <files>public/app.js, public/styles.css, test/module-ui-server.test.mjs, test/platform-ui-server.test.mjs</files>
  <action>Add admin/editor module configuration sheet with enable switches, required badges, up/down ordering, unsaved-change confirmation and transactional save. Keep published navigation unchanged. Enforce approved 12/14/16/28 type scale, 32px gutters, 76px header, Xugu warm desktop frame, local visualization scrolling, keyboard/reduced-motion and 1024/390 fallbacks.</action>
  <verify><automated>node --check public/app.js &amp;&amp; node --test test/module-ui-server.test.mjs test/platform-ui-server.test.mjs</automated></verify>
  <done>Viewer cannot see config; editor/admin can save draft-only order; desktop remains Xugu-aligned and responsive rules have static contract coverage.</done>
</task>
