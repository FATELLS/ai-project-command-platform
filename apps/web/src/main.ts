/**
 * Web Application Entry Point (skeleton)
 *
 * G11 will implement the real Vue 3 app.
 */

export function startWeb(): void {
  // TODO(G11): create Vue app, register router/plugins, mount to #app
  console.log("[web] skeleton ready — no Vue app mounted yet");
}
