# AI 系统设计契约

状态：`accepted design`

## 目标

LLM 将项目归属明确的纪要、计划、汇报、表格、数据、成果和人工表单转换为结构化 `ChangeProposal`。管理员审核后，系统把接受项合并到项目草稿，由固定模块渲染路线、甘特、任务、成果、风险和指标。

## 输入与证据

- 每份材料必须先归属 `projectId`。
- 提取结果拆分为可引用的证据块，记录文件、页/段/表位置和摘要。
- 模型上下文只能包含当前项目材料、当前发布版本、模板和更新意图。

## 输出

每个提案至少包含：

- `proposalId`
- `projectId`
- `baseVersion`
- `templateId` 与版本
- `updateType`
- `materialIds`
- `changes[]`

每项变化至少包含模块、操作、目标 ID、语义类型、数据载荷、证据引用、置信度和警告。

## 语义门槛

- `fact`：材料明确陈述的当前事实。
- `plan`：未来任务、目标、计划日期或预期产出。
- `suggestion`：模型建议，不得直接作为组织承诺。
- `unknown`：资料不足或冲突，必须进入待确认。

完成状态、指标值、日期和负责人等高影响字段必须有证据；没有证据时不得生成事实变更。

## 确定性校验

- JSON Schema；
- 项目、模板和模块白名单；
- 任务 ID、父子、依赖和循环；
- 日期顺序与甘特范围；
- 证据存在性和项目归属；
- 重复材料、重复任务和版本冲突；
- 高影响操作二次确认；
- 项目容量、频率、并发和 Token 配额。

## 权限边界

LLM 不得：

- 写入 `published`；
- 自动把提案合并到 `draft`；
- 生成并执行 HTML、JavaScript、CSS、SQL、Shell 或任意工具调用；
- 读取其他项目材料；
- 无来源补造完成率、成果、日期或责任人。

## 审核与发布

管理员必须能查看原值、建议值、证据、语义、置信度和警告，逐项接受、驳回或编辑。接受项在事务中合并到草稿；发布和回滚单独授权并进入审计。

## 评估

参考数据集覆盖纪要、计划 DOCX、汇报 PPTX、指标 XLSX、成果 PDF、图片、重复材料、冲突日期、循环依赖和跨项目混入。

上线门槛：

- 所有输出通过 Schema 或明确失败；
- 所有事实和完成状态可回到证据；
- 计划不会呈现为已完成；
- 跨项目隔离测试全部通过；
- AI 无法直接发布或执行代码；
- 同一材料重复处理不会静默产生重复任务。
